import { describe, expect, it, vitest, beforeEach } from 'vitest';
import { faker } from '@faker-js/faker';

describe('#propositon Entity tests', () => {
	beforeEach(() => {
		vitest.resetAllMocks();
		vitest.clearAllMocks();
	});
});
